<nav class="navbar navbar-expand-md p-1 mb-1 navbar navbar-custom">
<a class="navbar-brand" href="/">AGRI</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarCollapse" aria-controls="navbarCollapse" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarCollapse">
<%= link_to  "Farms", farms_path%>
<% if current_user.admin %>
<%= link_to  "Admin Dashboard", admins_path%>
<%end%>
</div>
</nav>

<table class="table table-striped" >
<thead>
    <tr>
        <th scope="col" class="bg-success" style="width:20%">User's Email</th>
        <th scope="col" class="bg-success" style="width:20%">Roll.EMP</th>
        <th scope="col" class="bg-success" style="width:20%">Make employee</th>
    </tr>
    </thead>
    <tbody>
        <% @users.each do |user|%>
        <tr>
        <td ><%= user.email %></td>
        <td> <%=user.emp%></td>
        <td><%= link_to  "Assign Emp ", admin_makeemp_path(user.id) %></td>
        </tr>
        <%end%>
       
    </tbody>
</table>

<div class="text-center">
<button class="butt" class="text-center" > <%= link_to 'Back', :back %></button> 
<button class="butt"> <%= link_to 'sign out' ,destroy_user_session_path,method: :delete  %></button> 
</div>