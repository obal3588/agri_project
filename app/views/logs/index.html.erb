<h1> <%=@farm.name %> </h1>
<h1> <%=@plot.name %> </h1>

<ol>
    <% @logs.each do |g| %>
       <li> <%= g.entry%> 
         <td> <%= g.updated_at%> </td>
        </li>
    <%end%>
</ol>

<button> <%= link_to "Add New Log", new_farm_plot_log_path(farm_id: @farm.id ,plot_id:@plot.id)  %> </button>

